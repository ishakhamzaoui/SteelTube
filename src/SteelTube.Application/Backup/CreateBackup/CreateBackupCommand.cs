namespace SteelTube.Application.Backup.CreateBackup
{
    /// <summary>No parameters -- backs up to the default location (SRS 16.1). A custom-location variant can be added later via Settings (SRS 17).</summary>
    public sealed class CreateBackupCommand
    {
    }
}