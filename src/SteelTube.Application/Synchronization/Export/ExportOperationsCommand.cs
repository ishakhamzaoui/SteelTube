namespace SteelTube.Application.Synchronization.Export
{
    /// <summary>No parameters -- exports every local operation (SRS 12.7: incremental export is an optional future optimization, not a requirement).</summary>
    public sealed class ExportOperationsCommand
    {
    }
}